/** Tailwind v4 is a PostCSS plugin; there is no tailwind.config.js — theme
 *  configuration lives in src/app/globals.css under @theme. */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}

export default config
